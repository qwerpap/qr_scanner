/// Статус подписки пользователя.
enum SubscriptionStatus {
  /// Подписка активна.
  active,

  /// Подписка неактивна или отсутствует.
  inactive,

  /// Статус неизвестен (ошибка при проверке).
  unknown,
}
